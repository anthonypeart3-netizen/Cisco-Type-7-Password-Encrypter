from passlib.hash import cisco_type7
import random


plain_text = ("password"
             "")


encrypted = cisco_type7.hash(plain_text)


print(f"Anthony's program took {plain_text} and turned it into")
print(encrypted)

